# Optional API key. The Met, Art Institute of Chicago, Cleveland, and
# Rijksmuseum all work without any key. Provide this to enable Harvard.
#
# Note: the server reads the process environment and does not load this file
# on its own. Either export the variable, run with `node --env-file=.env`, or
# set it in your MCP client's `env` block.

# Harvard Art Museums — request a free key:
# https://docs.google.com/forms/d/1Fe1H4nOhFkrLpaeBpLAnSrIMYvcAxnYWm0IU9a6IkFA/viewform
HARVARD_API_KEY=
